import { Session } from '../../session/session';
import { ConfigInterface } from '../../base-types';
import { RequestReturn } from '../../clients/http_client/types';
import { AccessTokenResponse } from './types';
export declare function createSession({ config, accessTokenResponse, shop, state, }: {
    config: ConfigInterface;
    accessTokenResponse: AccessTokenResponse;
    shop: string;
    state: string;
}): Session;
export declare function accessTokenResponse(postResponse: RequestReturn): AccessTokenResponse;
//# sourceMappingURL=create-session.d.ts.map