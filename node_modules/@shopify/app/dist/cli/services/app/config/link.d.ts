import { AppConfiguration, CurrentAppConfiguration } from '../../../models/app/app.js';
import { OrganizationApp } from '../../../models/organization.js';
import { ExtensionRegistration } from '../../../api/graphql/all_app_extension_registrations.js';
import { ExtensionSpecification } from '../../../models/extensions/specification.js';
import { Config } from '@oclif/core';
export interface LinkOptions {
    commandConfig: Config;
    directory: string;
    apiKey?: string;
    configName?: string;
}
export default function link(options: LinkOptions, shouldRenderSuccess?: boolean): Promise<AppConfiguration>;
export declare function mergeAppConfiguration(appConfiguration: AppConfiguration, remoteApp: OrganizationApp): CurrentAppConfiguration;
export declare function remoteAppConfigurationExtensionContent(configRegistrations: ExtensionRegistration[], configSpecifications: ExtensionSpecification[]): {
    [x: string]: unknown;
};
