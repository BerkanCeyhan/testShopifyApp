/// <reference types="jest" />
import { LogSeverity, ShopifyRestResources } from '@shopify/shopify-api';
import { SessionStorage } from '@shopify/shopify-app-session-storage';
import { MemorySessionStorage } from '@shopify/shopify-app-session-storage-memory';
import type { AppConfigArg } from '../config-types';
import type { FutureFlagOptions } from '../future/flags';
declare const TEST_CONFIG: {
    readonly apiKey: "testApiKey";
    readonly apiSecretKey: "testApiSecretKey";
    readonly scopes: any;
    readonly apiVersion: import("@shopify/shopify-api").ApiVersion.October23;
    readonly appUrl: "https://my-test-app.myshopify.io";
    readonly logger: {
        readonly log: jest.Mock<any, any, any>;
        readonly level: LogSeverity.Debug;
    };
    readonly isEmbeddedApp: true;
    readonly sessionStorage: MemorySessionStorage;
    readonly future: Required<{
        v3_webhookAdminContext?: true | undefined;
        v3_authenticatePublic?: true | undefined;
    }>;
};
export declare function testConfig<Overrides extends TestOverridesArg<Future>, Future extends FutureFlagOptions>({ future, ...overrides }?: Overrides & {
    future?: Future;
}): TestConfig<Overrides, Future>;
type Modify<Type1, Type2> = {
    [key in keyof Type2 as Type2[key] extends undefined ? never : key]: Type2[key];
} & {
    [key in keyof Type1 as key extends keyof Type2 ? never : key]: Type1[key];
};
type DefaultedFutureFlags<Future> = Modify<typeof TEST_CONFIG.future, Future>;
type TestOverrides<Future> = Partial<Omit<AppConfigArg<ShopifyRestResources, SessionStorage, DefaultedFutureFlags<Future>>, 'future'> & {
    future: Partial<DefaultedFutureFlags<Future>>;
}>;
type TestOverridesArg<Future> = undefined | TestOverrides<Future>;
type TestConfig<Overrides extends TestOverridesArg<Future>, Future> = Modify<typeof TEST_CONFIG, Overrides> & {
    future: Modify<typeof TEST_CONFIG.future, Future>;
};
export {};
//# sourceMappingURL=test-config.d.ts.map