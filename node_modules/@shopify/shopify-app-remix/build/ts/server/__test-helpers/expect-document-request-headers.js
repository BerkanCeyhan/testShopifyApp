"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.expectDocumentRequestHeaders = void 0;
var const_1 = require("../authenticate/const");
var const_2 = require("./const");
function expectDocumentRequestHeaders(response, isEmbeddedApp) {
    if (isEmbeddedApp === void 0) { isEmbeddedApp = true; }
    var headers = response.headers;
    if (isEmbeddedApp) {
        expect(headers.get('Content-Security-Policy')).toEqual("frame-ancestors https://".concat(encodeURIComponent(const_2.TEST_SHOP), " https://admin.shopify.com https://*.spin.dev;"));
        expect(headers.get('Link')).toEqual("<".concat(const_1.APP_BRIDGE_URL, ">; rel=\"preload\"; as=\"script\";"));
    }
    else {
        expect(headers.get('Content-Security-Policy')).toEqual("frame-ancestors 'none';");
    }
}
exports.expectDocumentRequestHeaders = expectDocumentRequestHeaders;
//# sourceMappingURL=expect-document-request-headers.js.map