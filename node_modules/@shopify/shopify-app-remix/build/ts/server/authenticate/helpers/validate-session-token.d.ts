import { JwtPayload } from '@shopify/shopify-api';
import type { BasicParams } from '../../types';
interface ValidateSessionTokenOptions {
    checkAudience?: boolean;
}
export declare function validateSessionToken({ api, logger }: BasicParams, token: string, { checkAudience }?: ValidateSessionTokenOptions): Promise<JwtPayload>;
export {};
//# sourceMappingURL=validate-session-token.d.ts.map