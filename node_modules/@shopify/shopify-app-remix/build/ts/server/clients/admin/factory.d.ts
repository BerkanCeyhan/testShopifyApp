import { Session, ShopifyRestResources } from '@shopify/shopify-api';
import { BasicParams } from '../../types';
import type { AdminApiContext } from './types';
interface RestClientOptions {
    params: BasicParams;
    session: Session;
    handleClientError?: (error: any) => Promise<void>;
}
export declare function adminClientFactory<Resources extends ShopifyRestResources = ShopifyRestResources>({ params, handleClientError, session, }: RestClientOptions): AdminApiContext<Resources>;
export {};
//# sourceMappingURL=factory.d.ts.map